<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="vn.supenient.camera">

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:configChanges="orientation|uiMode">

        <activity
            android:name=".previewlistimages.PreviewListImagesActivity" />

        <activity
            android:name=".gallery.SGalleryActivity" />

        <activity
            android:name=".SCameraActivity" />

        <activity android:name=".video.VideoPlayerActivity"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.app.album_maker.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
